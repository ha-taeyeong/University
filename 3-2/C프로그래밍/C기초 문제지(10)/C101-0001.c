1. 🔍 발생 원인
**malloc(), calloc(), realloc()**과 같은 함수로 힙(Heap) 메모리를 동적으로 할당한 후, 해당 메모리 주소를 가리키는 포인터를 잃어버리거나 (예: 포인터에 다른 주소를 덮어쓰거나 함수 종료 시 지역 포인터 변수가 사라짐), 사용 후 free() 함수를 호출하지 않았기 때문에 발생합니다.

2. 🛡️ 대응 방법
동적 할당 메모리는 사용 후 반드시 free()를 호출하여 명시적으로 해제해야 합니다.

할당된 메모리의 주소를 저장하는 포인터를 잃지 않도록 주의하며, 함수 종료 전 또는 메모리가 더 이상 필요하지 않을 때 해제 로직을 구현해야 합니다.

복잡한 프로그램에서는 **메모리 분석 도구(Memory Profiler, 예: Valgrind)**를 사용하여 누수 지점을 탐지하는 것이 효과적입니다.